const age = 2;

if(age >= 18) {
    console.log("Yeas you can vote");
} 
else if(age >= 80) {
    console.log("Yes you are an senior citizen");
} 
else {
    console.log("No you can't vote");
}

// Ternary Operator

let result = age >= 18 ? "YES" : "NO"

console.log(result);
